---
title: "MDX Sandbox"
description: "MDX sandbox page"
pubDate: "Jan 29 2023"
---

export const Thing = () => <>{'mdx'.toUpperCase()}</>

<div style={{ padding: '20px', backgroundColor: 'tomato' }}>
  <h3>This is <Thing /></h3>
</div>

```javascript
import Chart from './chart.astro'

<Chart />
```

import Chart from './chart.astro'

<Chart />

```javascript
<ul>
    {[...Array(5)].map((_, i) => <li key={i}>item {i+1}</li>)}
</ul>
```

<ul>
    {[...Array(5)].map((_, i) => <li key={i}>item {i+1}</li>)}
</ul>

```javascript
import About from '../../pages/about.md'

<About />
```

import About from '../../pages/about.md'

<About />

